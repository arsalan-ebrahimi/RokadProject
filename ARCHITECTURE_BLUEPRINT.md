# 🏗️ سند جامع معماری، الگوها و ساختار دقیق پروژه Rokad
> **نسخه رفرنس و پایه‌ی ساخت پروژه‌های جدید (Architecture Blueprint)**  
> این سند حاوی تمام جزئیات فنی، معماری ماژولار، استاندارد نام‌گذاری‌ها، سیستم طراحی، کامپوننت‌های UI، پایپ‌لاین احراز هویت و الگوهای CRUD فرانت‌اند و بک‌اند است تا بتوان هر پروژه و داشبورد جدیدی را عیناً بر پایه همین ساختار پیاده‌سازی کرد.

---

## ۱. ساختار کلان مخزن (Monorepo Layout)

پروژه به صورت مونورپو با دو بخش کاملاً مجزا و هماهنگ سازمان‌دهی شده است:

```
RokadProject/
├── API/                   # لایه بک‌اند (Node.js + Express + MongoDB)
│   ├── Middleware/        # میان‌افزارهای سراسری و اختصاصی (احراز هویت، مجوزها)
│   ├── Modules/           # ماژول‌های مستقل بر اساس دامنه کاری (Domain-Driven Modules)
│   ├── Public/            # فایل‌های استاتیک و تصاویر آپلود شده
│   ├── Utils/             # توابع کمکی (آپلود Multer، مستندات Swagger، پیامک و اعتبارسنجی)
│   ├── app.js             # پیکربندی اکسپرس، میان‌افزارها و مسیرها
│   ├── server.js          # اتصال به MongoDB و استارت سرور HTTP
│   ├── config.env         # متغیرهای محیطی بک‌اند
│   ├── security-config.js # تنظیمات امنیتی
│   ├── seedAdmin.js       # اسکریپت ساخت مدیر اولیه سیستم
│   └── SeoSeed..js        # اسکریپت سید تنظیمات اولیه سئو
│
└── Admin/                 # لایه فرانت‌اند داشبورد (React 19 + Vite + Tailwind CSS v4)
    ├── public/            # تصاویر استاتیک، آیکون‌ها و فونت‌های فارسی (IRANSansX, Vazirmatn)
    ├── src/
    │   ├── Components/    # کامپوننت‌های عمومی (سایدبار، فیلتر، جستجو، بارگذاری، سیستم UI)
    │   │   ├── AsidePro/  # سایدبار و منوی ناوبری تاشو با استایل اختصاصی
    │   │   ├── Filter/    # فیلتر داینامیک بر اساس کانفیگ آرایه
    │   │   ├── Search/    # باکس جستجوی دارای Debounce (400ms)
    │   │   ├── Loading/   # کامپوننت اسپینر بارگذاری متناسب با تم
    │   │   └── UI/        # کامپوننت‌های اتمیک طراحی (Button, Input, Textarea, Card, ImageUpload, ...)
    │   ├── Constants/     # مقادیر ثابت (آواتارهای پیش‌فرض)
    │   ├── Layout/        # پوسته اصلی داشبورد (شامل محافظت Route و لایه بندی سایدبار + محتوا)
    │   ├── Pages/         # صفحات و ویوهای ماژول‌ها (CRUD کامل هر بخش)
    │   ├── Routes/        # تنظیمات React Router v7 (ساختار تو در تو و محافظت شده)
    │   ├── Store/         # ریداکس تولکیت (Store و Slices مدیریت توکن احراز هویت)
    │   ├── Utils/         # کلاینت Axios، توابع Toast، دیالوگ تأیید SweetAlert و حل آدرس عکس
    │   ├── index.css      # کانفیگ تم Tailwind v4 و متغیرهای CSS و فونت‌ها
    │   ├── App.jsx        # روت اپلیکیشن و Toaster سراسری
    │   └── main.jsx       # مانت React در DOM و Redux Provider
    ├── vite.config.js     # کانفیگ بیلد Vite
    └── .env               # متغیرهای اتصال به API فرانت‌اند
```

---

## ۲. لایه بک‌اند (`API/`) - اصول و الگوهای پیاده‌سازی

### ۲.۱. بسته نرم‌افزاری و وابستگی‌ها (`package.json`)
- **Express (`^5.2.1`)**: فریمورک اصلی وب با معماری مدرن و پشتیبانی توکار از پرامیس‌ها.
- **Mongoose (`^8.18.1`)**: اتصال به دیتابیس MongoDB و مدیریت اسکیماها.
- **vanta-api (`^1.5.2`)**: موتور پردازش کوئری و خطا:
  - `catchAsync`: رپ کردن خودکار کنترلرها جهت حذف بلوک‌های تکراری `try/catch`.
  - `HandleERROR`: ساخت خطاهای کنترل‌شده HTTP با کد وضعیت دلخواه.
  - `catchError`: میدل‌ویر نهایی مدیریت خطاهای اکسپرس.
  - `ApiFeatures`: موتور جامع فیلترینگ پیشرفته، سرچ روی فیلدها (`?q=...`)، مرتب‌سازی (`?sort=-_id`)، صفحه‌بندی (`?page=1&limit=10`)، انتخاب فیلدها (`?fields=...`) و پیوند دیتابیس (`?populate=...`).
- **Joi (`^18.2.3`)**: اعتبارسنجی ورودی‌ها با پیغام‌های سفارشی فارسی و رجکس امنیتی:
  ```javascript
  const safeTextRegex = /^[\u0600-\u06FF\sA-Za-z0-9\-\_،؛؟!.:«»",;?()\u200c\u200d]+$/;
  ```
- **jsonwebtoken (`^9.0.2`)** و **bcryptjs (`^3.0.2`)**: تولید توکن JWT (اعتبار ۹۰ روزه) و هش کلمات عبور با salt rounds برابر ۱۲.
- **multer (`^2.0.2`)**: ذخیره فایل‌ها در دایرکتوری `Public/` با فرمت نام‌گذاری `${originalName}_${Date.now()}.${ext}` و کنترل پسوند مجاز (`jpeg, jpg, png, svg, webp`).
- **swagger-ui-express** و **swagger-jsdoc**: ساخت مستندات اتوماتیک API در مسیر `/api-docs`.

### ۲.۲. استاندارد نام‌گذاری فایل‌ها در هر ماژول بک‌اند (`Modules/[FeatureName]/`)
ساختار هر ماژول شامل ۴ فایل کلیدی با نام‌گذاری دقیق است:
1. **`[FeatureName]Md.js` (Model)**: تعریف اسکیمای مانگوس با پیام‌های خطای فارسی و فیلدهای لازم.
2. **`[FeatureName]Cn.js` (Controller)**: کنترلر شامل توابع `create`, `getAll`, `getOne`, `update`, `remove` درون `catchAsync`.
3. **`[FeatureName]Validation.js` (Validation)**: شامل اسکیماهای Joi برای ساخت (`create...Validator`) و به‌روزرسانی (`update...Validator`).
4. **`[FeatureName].js` (Routes + Swagger)**: تعریف روت‌ها با متدهای زنجیره‌ای و انوتیشن کامل Swagger JSDoc.

```javascript
// الگوی استاندارد روت در [FeatureName].js
const router = Router();

router.route("/")
  .get(getAll)
  .post(IsLogin, IsAdmin, validateRequest(createValidator), create);

router.route("/:id")
  .get(getOne)
  .patch(IsLogin, IsAdmin, validateRequest(updateValidator), update)
  .delete(IsLogin, IsAdmin, remove);
```

### ۲.۳. سازوکار احراز هویت و کنترل دسترسی (Auth Pipeline)
1. **`Middleware/ExportValidation.js`**:
   در تمام درخواست‌ها به صورت عمومی اجرا شده، هدر `Authorization: Bearer <token>` را خوانده و با کلید `JWT_SECRET` اعتبارسنجی می‌کند. در صورت معتبر بودن، `req.userId` و `req.role` ست می‌شوند (در غیر اینصورت null می‌شوند).
2. **`Middleware/IsLogin.js`**:
   بررسی وجود `req.userId` و در غیر اینصورت بازگرداندن کد ۴۰۱ با پیام «لطفا ابتدا وارد حساب کاربری خود شوید».
3. **`Middleware/IsAdmin.js`**:
   بررسی شرط `req.role === "admin" || req.role === "superAdmin"` و در غیر اینصورت بازگرداندن کد ۴۰۳ با پیام «شما مجاز به انجام این عملیات نیستید».

---

## ۳. لایه فرانت‌اند (`Admin/`) - معماری و دیزاین سیستم

### ۳.۱. استک فنی فرانت‌اند
- **React 19** + **Vite 7**: سرعت فوق‌العاده در توسعه و بیلد.
- **Tailwind CSS v4**: استفاده از سینتکس مدرن `@theme` درون `index.css` بدون نیاز به فایل کانفیگ حجیم.
- **Redux Toolkit**: ذخیره توکن احراز هویت در `authSlice` و سینک خودکار با `localStorage`.
- **React Router v7**: ساختار روتینگ تو در تو (`Layout` -> ساب‌روت‌های هر موجودیت).
- **Formik + Yup**: مدیریت کامل فرم‌ها، اعتبارسنجی بلادرنگ و مدیریت آپلود عکس.
- **react-hot-toast**: سیستم اطلاع‌رسانی راست‌چین و مدرن با تم اختصاصی.
- **sweetalert2**: پاپ‌آپ‌های مدرن تایید قبل از حذف داده با استایل مینیمال.

### ۳.۲. سیستم رنگ و توکن‌های طراحی (`index.css`)
- **Primary**: `#52b8ab` (سبز زمردی/کله‌غازی برای اکشن‌ها و هایلایت‌های فعال).
- **Secondary**: `#1b234d` (سرمه‌ای عمیق برای هدرها، تیترها و فونت‌های سنگین).
- **Background**: `#f8fafc` (خاکستری روشن برای پس‌زمینه کل پنل).
- **Surface**: `#ffffff` (سفید یکدست برای کارت‌ها و سطوح فرم).
- **Borders**: `#e2e8f0` و `#d6eeeb` (بردرهای ملایم و مدرن).
- **تایپوگرافی**: خانواده فونت `IRANSansX` و `Vazirmatn` همراه با فونت اعداد فارسی `IRANSansXFaNum`.

### ۳.۳. کامپوننت‌های اتمیک سیستم طراحی (`src/Components/UI/`)
همه کامپوننت‌ها از ساختار Barrel Export در `src/Components/UI/index.js` بهره می‌برند:
- **`Button`**: دکمه‌های کامپوننت‌پذیر با سایزهای (`sm`, `md`, `lg`, `tall`, `icon`)، استایل‌های متنوع (`primary`, `secondary`, `outline`, `danger`, `ghost`, `primary-subtle`) و مدیریت لودینگ یکپارچه با اسپینر `Loading`.
- **`Input` & `Textarea`**: المان‌های استاندارد فرم با قابلیت دریافت لیبل، آیکون، متن راهنما و نمایش پویای خطاهای Formik.
- **`PageHeader`**: نوار هدر بالای هر صفحه با عنوان، توضیح، دکمه بازگشت تاریخچه (`backTo`) و جایگاه اکشن‌ها و فیلترها.
- **`ImageUpload`**: کامپوننت درگ-اند-دراپ جهت آپلود عکس، پیش‌نمایش تصویر موجود یا جدید، اعتبارسنجی فرمت و نمایش خطاها.
- **`Card`**: کانتینر سفید با انحنا و بردر ظریف و حالت اختیاری `hoverable`.
- **`Select`, `Checkbox`, `Badge`**: کامپوننت‌های مکمل فرم‌ها.

### ۳.۴. الگوهای رفتاری صفحات (CRUD Lifecycle Patterns)
در هر بخش جدید از داشبورد، یک دایرکتوری در `Pages/[FeatureName]/` ایجاد می‌شود شامل:
1. **`index.jsx`**: والد و نگهدارنده `<Outlet />`.
2. **`[FeatureName]Page/index.jsx`**:
   - کامپوننت جستجوی دارای تاخیر ۴۰۰ میلی‌ثانیه‌ای (`Search`).
   - فیلتر پویا (`Filter`) با پیکربندی تک‌خطی.
   - گرید ریسپانسیو از کارت‌ها (`[FeatureName]Card`).
   - متد دیالوگ تایید حذف با `confirm()` و به‌روزرسانی زنده لیست در استیت محلی.
3. **`Create[FeatureName]/index.jsx`**:
   - پیاده‌سازی فرم با Formik + Yup.
   - پایپ‌لاین آپلود عکس (ارسال به `/api/upload` و سپس ارسال آبجکت کامل رکورد به دیتابیس).
   - هدایت خودکار با پیام موفقیت توسط `Notify.success`.
4. **`Update[FeatureName]/index.jsx`**:
   - واکشی رکورد از سرور بر اساس `id` دریافتی از `useParams()`.
   - پرکردن فیلدها با `enableReinitialize: true`.
   - حفظ تصویر قبلی در صورت عدم انتخاب تصویر جدید توسط کاربر.

---

## ۴. ابزارهای کلیدی فرانت‌اند (`Admin/src/Utils/`)
- **`axiosInstance.js`**: تزریق خودکار توکن JWT Bearer در تمامی رکوئست‌ها، بازگرداندن مستقیم دیتا در `response.data` و تبدیل خطاهای سرور به پیام‌های قابل‌فهم فارسی.
- **`notify.js`**: هلپر قدرتمند متصل به react-hot-toast با توابع کمکی `Notify.success()`, `Notify.error()`, `Notify.warning()`, `Notify.info()`, `Notify.loading()`.
- **`confirm.js`**: متد پرامیس‌محور (`await confirm(title, desc, confirmBtn)`) برای باز کردن مودال‌های تایید SweetAlert2.
- **`getImageUrl.js`**: حل مسیر کامل عکس‌ها (پشتیبانی از آواتارهای پیش‌فرض `/default-avatars/...` و فایل‌های آپلودی سرور).

---

## ۵. راهنمای گام‌به‌گام پیاده‌سازی داشبورد جدید

وقتی فیچرها و مدل‌های پروژه جدید خود را تعریف کنید، این نقشه راه دقیقاً اعمال خواهد شد:
1. **گام اول: پیکربندی و دیتابیس**:
   - تنظیم نام پایگاه‌داده جدید در `config.env`.
   - در صورت نیاز، به‌روزرسانی مدل کاربر `UserMd.js` و سید اولیه مدیریت در `seedAdmin.js`.
2. **گام دوم: ایجاد ماژول‌های بک‌اند**:
   - ساخت فولدر ماژول به نام موجودیت (مثلاً `Products`, `Orders`, `Tickets`).
   - ساخت ۴ فایل مطابق الگوی دقیق (`Md.js`, `Cn.js`, `Validation.js`, `.js`).
   - افزودن مسیر در `app.js`.
3. **گام سوم: ایجاد صفحات فرانت‌اند**:
   - ساخت پوشه در `Admin/src/Pages/[EntityName]/` با ساختار `Page`, `Create`, `Update`, `Card`.
   - تعریف روت‌های فرزند در `Admin/src/Routes/index.jsx`.
   - افزودن گزینه به منوی کناری در `AsideMenuPro` و آیکون سریع در صفحه اصلی `Home`.
4. **گام چهارم: تست انتها به انتها (E2E Verification)**:
   - تست لاگین، واکشی دیتا با جستجو و فیلتر، ایجاد رکورد با آپلود عکس، ویرایش و حذف.
